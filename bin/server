#!/usr/bin/env sh

# To turn on Middleman logging, add `--verbose` to the command below
yarn nodemon \
  --watch . \
  --watch bin \
  --watch config \
  --ext rb,erb \
  --exec "bundle exec middleman"
